// Building objects

canvas = new Canvas();
map = new Map();
notification = new Notification();
timer = new Timer(40);
configurator = new Configurator();
game = new Game();

// Settings up the configuration interface

configurator.init();